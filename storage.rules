rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Phase 1: default deny. Phase 4 opens admin uploads + signed-URL downloads.
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
